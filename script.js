const slides = document.querySelectorAll('.slide'), prog = document.getElementById('prog'), curSlide = document.getElementById('curSlide'), sh = document.getElementById('sh');
      const obs = new IntersectionObserver(e => { e.forEach(en => { if (en.isIntersecting) { en.target.classList.add('visible'); curSlide.textContent = Array.from(slides).indexOf(en.target) + 1 } }) }, { threshold: .18 });
      slides.forEach(s => obs.observe(s));
      window.addEventListener('scroll', () => { const t = window.scrollY, h = document.documentElement.scrollHeight - innerHeight; prog.style.width = (h > 0 ? (t / h) * 100 : 0) + '%'; sh.classList.toggle('hide', t > 100) });
      document.addEventListener('keydown', e => { const ids = Array.from(slides).map(s => s.id), ci = ids.findIndex(id => { const r = document.getElementById(id).getBoundingClientRect(); return r.top >= -200 && r.top < innerHeight / 2 }); if (e.key === 'ArrowDown' || e.key === 'ArrowRight') { e.preventDefault(); document.getElementById(ids[Math.min(ci + 1, ids.length - 1)]).scrollIntoView({ behavior: 'smooth' }) } if (e.key === 'ArrowUp' || e.key === 'ArrowLeft') { e.preventDefault(); document.getElementById(ids[Math.max(ci - 1, 0)]).scrollIntoView({ behavior: 'smooth' }) } if (e.key === 'f' || e.key === 'F') { if (!document.fullscreenElement) document.documentElement.requestFullscreen(); else document.exitFullscreen() } });